import React from 'react';

const Header = () => {
    return (
        <header className={'bg-white px-4 py-8 lg:px-14'}>
            <div className={'max-w-screen-2xl mx-auto'}>
                header here
            </div>
        </header>
    );
};

export default Header;